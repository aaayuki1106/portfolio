# 梁睿雯 · 作品集

## 部署到 GitHub Pages

1. 在 GitHub 新建一个仓库，仓库名随意（例如 `portfolio`）
2. 把本文件夹里的 **所有文件**（`index.html` + `assets` 文件夹）上传到仓库根目录
3. 进入仓库 Settings → Pages → Source 选 `main` 分支，点 Save
4. 等 30 秒，访问 `https://你的用户名.github.io/portfolio/`

## 更新网站

以后修改 `index.html` 或替换 `assets/` 里的素材，重新上传覆盖即可。
